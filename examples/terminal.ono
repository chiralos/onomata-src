"nl"  (stdout "\n" write pop) def

"cls" (stdout term/cls pop) def

"cursor-to" ( # (x y ->)
  stdout bry term/cursor-to pop
) def

"set-raw" (stdin term/raw pop) def
"set-nonblocking" (stdin term/raw-nonblocking pop) def
"reset-term" (stdin term/reset pop) def
